# NestJS REST API URL. This variable is server-only and must not use NEXT_PUBLIC_.
HARNESS_API_URL="http://127.0.0.1:4000"

# MCP endpoint URL. This variable is server-only and must not use NEXT_PUBLIC_.
HARNESS_MCP_URL="http://127.0.0.1:4000/mcp"
